import React from 'react';
import injectSheet from 'react-jss';

export const TREND_PERCENT = {
  volunteer: 'volunteer_percent',
  volunteer_hours: 'volunteer_hours_percent',
  meeting: 'meeting_percent',
  community: 'community_percent',
  donated: 'donated_percent',
  voted: 'vote_percent',
  boycotted: 'product_percent',
  registered: 'registered_percent',
  group: 'group_percent',
  organization: 'organization_percent',
};

const DonutGraph = (props) => {
  const {
    classes,
  } = props;



  return (

  );
}



export default injectSheet(styles)(DonutGraph);
