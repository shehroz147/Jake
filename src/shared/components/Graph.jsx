import React, { PropTypes } from 'react';

const Graph = props => {
  return (
    <div>
      {/* Passing of props from state information to produce a horivontal barGraph */}
    </div>
  );
};

Graph.propTypes = {};

export default Graph;
